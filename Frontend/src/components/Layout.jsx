import { NavLink,useNavigate } from "react-router-dom";
import { LayoutDashboard,CalendarDays,Users,Building2,LogOut,Clock3 } from "lucide-react";
import { useAuth } from "../context/AuthContext";
export default function Layout({children}){const {user,logout}=useAuth();const nav=useNavigate();const hr=user?.role==="HR";const links=hr?[['/dashboard','Dashboard',LayoutDashboard],['/leaves','Leave Requests',Clock3],['/employees','Employees',Users],['/departments','Departments',Building2]]:[['/dashboard','My Dashboard',LayoutDashboard],['/leaves','My Leaves',CalendarDays]];return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">A</div><div><strong>ABC CORP</strong><span>PEOPLE OPERATIONS</span></div></div><div className="workspace-label">WORKSPACE</div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`nav-item ${isActive?'active':''}`}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><div className="security-pill">● Secure workspace</div><button className="sidebar-logout" onClick={()=>{logout();nav('/login')}}><LogOut size={16}/> Sign out</button></div></aside><main className="main-content"><header className="topbar"><div><span className="eyebrow">{hr?'HR OPERATIONS':'EMPLOYEE SELF SERVICE'}</span><h1>{hr?'Leave & People Management':'My Leave Workspace'}</h1></div><div className="profile"><div className="avatar">{user?.name?.slice(0,1).toUpperCase()}</div><div><strong>{user?.name}</strong><small>{user?.role}</small></div></div></header>{children}</main></div>}
