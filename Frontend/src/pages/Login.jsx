import { useState } from "react";
import { Link,useLocation,useNavigate } from "react-router-dom";
import { ShieldCheck,ArrowRight } from "lucide-react";
import { useAuth } from "../context/AuthContext";
export default function Login(){const {login}=useAuth();const nav=useNavigate();const loc=useLocation();const [form,setForm]=useState({email:"",password:""});const [error,setError]=useState("");const [busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setError("");setBusy(true);try{const u=await login(form);nav(loc.state?.from?.pathname||"/dashboard",{replace:true})}catch(err){setError(err.response?.data?.error||"Unable to sign in. Check your credentials.")}finally{setBusy(false)}};return <div className="auth-page"><div className="auth-panel"><div className="auth-brand"><div className="brand-mark">A</div><div><strong>ABC CORP</strong><span>EMPLOYEE LEAVE PORTAL</span></div></div><div className="auth-copy"><span className="eyebrow">SECURE ACCESS</span><h1>Welcome back</h1><p>Sign in to manage leave requests, approvals and employee records.</p></div><form className="auth-card" onSubmit={submit}>{error&&<div className="error-banner">{error}</div>}<label>Work email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@company.com"/></label><label>Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Enter your password"/></label><button className="primary-btn auth-submit" disabled={busy}>{busy?"Signing in...":"Sign in"}<ArrowRight size={16}/></button><div className="auth-note"><ShieldCheck size={14}/> JWT-protected company workspace</div></form><div className="demo-hint"><span>HR demo</span><strong>hr@abccorp.com / hr123456</strong></div><p className="auth-switch">New employee? <Link to="/register">Create your account</Link></p></div></div>}
